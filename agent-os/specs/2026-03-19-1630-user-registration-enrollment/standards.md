# Standards

No standards are currently indexed (`agent-os/standards/index.yml` is empty).

Patterns followed from existing codebase:
- CSS Modules for component styling (matching `HomePage.module.css` pattern)
- `useNavigate` from react-router-dom for navigation
- JWT stored in `localStorage` as `token`
- Express controllers follow `async (req, res)` pattern with try/catch
